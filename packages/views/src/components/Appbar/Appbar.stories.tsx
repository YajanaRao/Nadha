import React from "react";
import { Appbar } from "./AppBar";
import { Screen } from "../Screen";

export const AppBar = () => (
  <Screen>
    <Appbar />
  </Screen>
);

export default {
  title: "Appbar",
};
